import { ModuleSettings } from '@healthcare/core';

export class TemperatureSettings extends ModuleSettings {
  override readonly moduleId = 'Temperature';
  override readonly moduleName = 'Temperature';
}
